#include <stdlib.h>
#include <stdio.h>

int main(void){

    system("ps u");

    /*
    
    a → mostra processos de todos os utilizadores, não apenas os meus;

	u → mostra o utilizador dono de cada processo e mais detalhes (CPU, memória, etc.);

	x → mostra processos que não estão ligados a um terminal (por exemplo, serviços do sistema).

    */

    return 0;
}